name: SHACL SARIF POC

on:
  workflow_dispatch:
  push:
    branches: [shacl-sarif-poc]
    paths:
      - Instance/**
      - buildScripts/validate_shacl_sarif.py
      - .github/workflows/shacl_sarif_poc.yml
      - pyproject.toml

permissions:
  contents: read
  security-events: write

jobs:
  shacl-sarif:
    name: Validate and upload SHACL results to code scanning
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Validate instances, export grouped SARIF + full SHACL reports
        run: uv run buildScripts/validate_shacl_sarif.py

      # one upload per SARIF run — code scanning no longer combines
      # multiple runs uploaded under the same category
      - name: Upload EQ demo SARIF to code scanning
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: reports/20220615T2230Z__Svedala_EQ_1.sarif
          category: shacl-eq-demo

      - name: Upload NCP RA SARIF to code scanning
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: reports/DC-Espheim-Svedala_RA.sarif
          category: shacl-ncp-ra

      - name: Upload SARIF + full SHACL reports as run artifact
        uses: actions/upload-artifact@v4
        with:
          name: shacl-reports
          path: |
            reports/*.sarif
            reports/*-shacl-report.*
            reports/summary.md

      - name: Run summary with link to code scanning results
        run: cat reports/summary.md >> "$GITHUB_STEP_SUMMARY"
