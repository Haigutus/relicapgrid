# Correction of generation production due to a power flow limit violation

The following example still needs to be integrated in ReliCapGrid:

- Open the anonymized IGM __

- Import the SPS file from Remedial action profile *test_SPS.xml*

- Check if *Lines 1,2,3 and Transformer 1* are in operation

- Check if *Generators 1,2,3* are in operation

- Check the load flow on *AT600 terminal*

- Check if SIPS was triggered and if correctly (correct scenario, check production of generators and terminal flow at *AT600*)

The test data consists of an anonymised IGM file (still to be integrated) and a Remedial Action Profile file that contains two different Stages of a single SPS.
